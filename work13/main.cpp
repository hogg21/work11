#include <windows.h>

/* This is where all the input to the window goes to */
class Sin {
	private:
		int a, b
	public:
		sin() {
			return b / a;
		}
};

class Cos {
	Sin s;
	private:
		int a;
	protected:
		cos() {
			return a / s.sin();
		}
};
